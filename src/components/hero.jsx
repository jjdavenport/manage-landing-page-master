import hero from "../assets/illustration-intro.svg";

const Hero = () => {
  return (
    <>
      <section className="flex justify-center">
        <img src={hero} />
      </section>
    </>
  );
};

export default Hero;
